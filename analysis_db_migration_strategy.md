# DB 마이그레이션 전략 분석 보고서

> 기존 ERD(legacy) vs 신규 프로토타입(html) 데이터 구조 비교 및 전략 제안

---

## 1. 분석 대상 요약

### 1.1 기존 시스템 (Legacy ERD)
- **테이블 수**: 약 30개
- **DBMS**: Microsoft SQL Server 2008
- **스키마**: `dbo` (메인), `e4ds` (회원/기업)
- **핵심 엔티티**: 콘테스트, 프로젝트, 아티클, 커뮤니티 게시판, 설문, 키워드, 상품

### 1.2 신규 프로토타입 (HTML)
- **페이지 수**: 28개
- **핵심 엔티티**: 프로젝트(개인/콘테스트), 퀘스트 시스템, 커뮤니티(4가지 유형), 팀, 기술 스택, 소셜 인증

---

## 2. 구조 변경점 상세 비교

### 2.1 변경 없이 재사용 가능한 영역 (호환)

| 기존 테이블 | 신규 대응 | 호환도 |
|---|---|---|
| `MK_CONTEST` | 콘테스트 마스터 | **높음** — 기본 필드 대부분 유지 |
| `MK_CONTEST_STEP` | 퀘스트 단계 | **높음** — 구조 동일, 필드 확장 필요 |
| `MK_CONTEST_REGISTER` | 콘테스트 참가 | **높음** — CR_ADD 필드 체계 조정 필요 |
| `MK_ARTICLE` | 프로젝트 아티클 | **중간** — 기본 구조 유지, 미디어 연결 방식 변경 |
| `MK_ARTICLE_REPLY` | 댓글 | **높음** — 스레드 구조 동일 |
| `MK_ATTACH` | 첨부파일 | **높음** — 그대로 사용 가능 |
| `MK_PRIZE` / `MK_PRIZE_REG` | 수상 | **높음** — 변경 없음 |
| `MK_BNR` | 배너 | **높음** — 변경 없음 |
| `MAKE_KEYWORD` / `MK_ARTICLE_KEYWORD` | 태그/키워드 | **중간** — 태그 시스템으로 확장 필요 |
| `e4ds.semi_member` | 사용자 | **중간** — 소셜 로그인, 프로필 확장 필요 |
| `e4ds.semi_company` | 기업 | **높음** — 변경 없음 |

### 2.2 확장/수정이 필요한 영역

| 영역 | 기존 구조 | 신규 요구사항 | 변경 규모 |
|---|---|---|---|
| **프로젝트** | `MK_PROJECT` 단일 테이블 | 카테고리, 난이도, 목표, 예상기간, 기술스택 추가 | **중간** — 컬럼 추가 또는 상세 테이블 분리 |
| **커뮤니티** | `MC_BOARD` (Free/QNA/Event 3유형) | 질문/진행공유/팀모집/자료공유 4유형 + 프로젝트 연결 | **중간** — TYPE 확장 + 모집 전용 컬럼 |
| **태그 시스템** | `MAKE_KEYWORD` (단순 검색어) | 프로젝트·게시물·사용자에 태그 연결, 카테고리 분류 | **중간** — 다대다 매핑 테이블 추가 |
| **사용자 프로필** | `semi_member` (기본 정보) | 소셜 로그인, 스킬, 팔로워, 통계, 커버이미지 | **중간~큼** — 프로필 확장 테이블 필요 |

### 2.3 완전히 새로운 영역

| 신규 기능 | 필요 테이블 | 기존 대응 | 비고 |
|---|---|---|---|
| **팀 시스템** | `MK_TEAM`, `MK_TEAM_MEMBER` | 없음 | 프로젝트-팀 연결, 역할 관리 |
| **팀 모집** | `MK_RECRUITMENT`, `MK_RECRUITMENT_POSITION` | 없음 | 커뮤니티 게시판 확장 또는 별도 테이블 |
| **기술 스택** | `MK_TECHNOLOGY`, `MK_PROJECT_TECH`, `MK_USER_SKILL` | `MK_PLATFORM`(일부) | HW/SW 구분, 사용자 스킬 매핑 |
| **소셜 인증** | `MK_USER_AUTH` | 없음 | Google, Kakao 연동 |
| **북마크/스크랩 통합** | `MK_BOOKMARK` | `MK_ARTICLE_ACT`(일부) | 프로젝트·게시물 통합 스크랩 |
| **갤러리/미디어** | `MK_MEDIA` | `MK_ATTACH`(일부) | 인라인 이미지, 갤러리 지원 |
| **팔로우** | `MK_FOLLOW` | 없음 | 사용자 간 팔로우/팔로잉 |

---

## 3. 전략 비교 분석

### 전략 A: 기존 테이블 확장 (Incremental Migration)

**방법**: 기존 테이블에 컬럼 추가 + 신규 테이블 추가, 데이터 이관 없이 운용

#### 장점
- 기존 데이터 **무손실 보존**, 이관 작업 불필요
- 기존 서버 코드(ASP 등) 영향 **최소화**
- 개발 기간 **짧음** (신규 테이블만 CREATE, 기존 테이블은 ALTER)
- 운영 리스크 **낮음** (롤백 용이)

#### 단점
- `CR_ADD1~5` 같은 **범용 컬럼 체계가 그대로 잔존** — 의미 파악 어려움
- `MC_BOARD`에 팀 모집 컬럼 추가 시 **NULL 비율 급증** (게시판 유형별 사용 컬럼이 다름)
- `MK_PLATFORM`과 신규 `MK_TECHNOLOGY`의 **역할 중복** 발생
- 시간이 지날수록 **구조 복잡도 누적** (기술 부채)
- `varbinary(100)` 같은 레거시 타입이 유지됨 (MC_BOARD.NICKNAME 등)

#### 예상 작업량
| 항목 | 수량 |
|---|---|
| ALTER TABLE (컬럼 추가) | 5~7개 테이블 |
| CREATE TABLE (신규) | 6~8개 테이블 |
| 데이터 이관 | 없음 |
| 기존 코드 수정 | 최소 |

---

### 전략 B: 신규 스키마 설계 + 데이터 이관 (Full Redesign)

**방법**: 새 스키마 설계 후, 기존 데이터를 변환·이관

#### 장점
- **정규화/명명 규칙 통일** — `CR_ADD1~5` 제거, 명확한 컬럼명
- 신규 기능에 **최적화된 구조** 설계 가능
- **불필요 테이블·컬럼 정리** (MK_PRESURVEY의 PS_ANS1~20 등)
- **데이터 타입 현대화** (varbinary → nvarchar, datetime → datetime2 등)
- 장기 유지보수 비용 **절감**

#### 단점
- **데이터 이관 스크립트** 개발 필요 (30+테이블, FK 관계 복잡)
- 이관 중 **데이터 정합성 검증** 부담 큼
- 기존 서버 코드 **전면 수정** 필요 (쿼리, 프로시저 등)
- SQL Server 2008 환경에서 **마이그레이션 도구 제한** (SSIS 등)
- 개발·테스트 기간 **2~3배 소요**
- 운영 전환 시 **다운타임** 발생

#### 예상 작업량
| 항목 | 수량 |
|---|---|
| CREATE TABLE (전체) | 25~30개 테이블 |
| 데이터 이관 스크립트 | 30+개 |
| 정합성 검증 쿼리 | 30+개 |
| 기존 코드 전면 수정 | 전체 |

---

## 4. 정량 비교표

| 평가 기준 | 전략 A (확장) | 전략 B (재설계) | 비고 |
|---|---|---|---|
| 개발 기간 | **짧음** | 길음 | A가 2~3배 빠름 |
| 데이터 안전성 | **높음** | 중간 | 이관 과정에서 위험 존재 |
| 운영 리스크 | **낮음** | 높음 | B는 다운타임 필요 |
| 구조 품질 | 중간 | **높음** | B가 장기적으로 우수 |
| 유지보수성 | 중간 | **높음** | B가 기술 부채 적음 |
| 기존 코드 영향 | **최소** | 전면 수정 | A가 압도적 우위 |
| 확장 용이성 | 중간 | **높음** | B가 미래 대응 유리 |
| 팀 부담 | **낮음** | 높음 | |

---

## 5. 결론 및 권고

### 권고안: **전략 A (기존 테이블 확장)** 를 기본으로 하되, 부분 재설계 병행

#### 근거

1. **기존 구조의 호환도가 높다**
   - 30개 테이블 중 약 70%(21개)는 수정 없이 또는 컬럼 추가만으로 재사용 가능
   - 핵심 도메인(콘테스트, 프로젝트, 아티클, 댓글, 첨부파일)의 구조가 거의 동일

2. **완전 재설계의 비용 대비 효과가 낮다**
   - 기존 데이터 이관 + 서버 코드 전면 수정 비용이 구조 개선 이득보다 큼
   - SQL Server 2008 환경에서 마이그레이션 리스크가 높음

3. **신규 기능은 독립적 테이블로 추가 가능**
   - 팀, 기술 스택, 소셜 인증, 팔로우 등은 기존 테이블과 FK로만 연결
   - 기존 구조를 건드리지 않고 신규 테이블만 추가하면 됨

#### 구체적 실행 방안

```
[Phase 1] 기존 테이블 확장 (ALTER TABLE)
├── MK_PROJECT: 카테고리, 난이도, 목표, 예상기간 컬럼 추가
├── MC_BOARD: CB_TYPE 확장 (4→팀모집/자료공유), 모집 관련 컬럼 추가
├── semi_member: 프로필 이미지, 커버이미지, 자기소개 컬럼 추가
├── MAKE_KEYWORD: 카테고리, 사용횟수 컬럼 추가
└── MK_CONTEST_STEP: 완료율, 콘텐츠 타입 컬럼 추가

[Phase 2] 신규 테이블 생성 (CREATE TABLE)
├── MK_TEAM (팀 마스터)
├── MK_TEAM_MEMBER (팀원 + 역할)
├── MK_TECHNOLOGY (기술 스택 마스터, HW/SW 구분)
├── MK_PROJECT_TECH (프로젝트-기술 스택 매핑)
├── MK_USER_SKILL (사용자-기술 매핑)
├── MK_USER_AUTH (소셜 인증 연동)
├── MK_FOLLOW (팔로우/팔로잉)
├── MK_BOOKMARK (통합 북마크)
└── MK_POST_TAG (게시물-태그 매핑)

[Phase 3] 레거시 정리 (선택, 추후 진행)
├── MK_PLATFORM → MK_TECHNOLOGY 통합 (데이터 이관 후 폐기)
├── CR_ADD1~5 → MK_CONTEST_REG_ITEM 정규화
└── MC_BOARD.NICKNAME varbinary → nvarchar 변환
```

#### 이 전략의 핵심 이점

- **즉시 개발 착수 가능** — 기존 데이터와 코드를 유지하면서 병행 개발
- **점진적 개선** — Phase 3에서 레거시 정리를 시간 여유 있을 때 진행
- **롤백 안전** — 기존 테이블의 컬럼 추가는 기존 기능에 영향 없음
- **데이터 무손실** — 이관 작업 없으므로 정합성 이슈 원천 차단

---

## 부록: 변경 영향도 매트릭스

| 기존 테이블 | Phase 1 변경 | Phase 2 연결 | Phase 3 정리 |
|---|---|---|---|
| `MK_CONTEST` | - | - | - |
| `MK_CONTEST_STEP` | 컬럼 추가 | - | - |
| `MK_CONTEST_REGISTER` | - | - | ADD 필드 정규화 |
| `MK_PROJECT` | 컬럼 추가 | TECH, TEAM FK | - |
| `MK_ARTICLE` | - | - | - |
| `MK_ARTICLE_REPLY` | - | - | - |
| `MK_ARTICLE_ACT` | - | BOOKMARK 통합 | 폐기 검토 |
| `MC_BOARD` | TYPE 확장 | TAG 매핑 | NICKNAME 타입 변경 |
| `MAKE_KEYWORD` | 컬럼 추가 | POST_TAG | - |
| `MK_PLATFORM` | - | - | TECHNOLOGY 통합 |
| `semi_member` | 프로필 컬럼 추가 | AUTH, SKILL, FOLLOW FK | - |
| `MK_ATTACH` | - | - | - |
| `MK_PRIZE` | - | - | - |
| `MK_BNR` | - | - | - |
| `MK_PRESURVEY` | - | - | ANS1~20 정규화 검토 |

---

*작성일: 2026-04-06*
*분석 기준: legacy/ ERD 파일 및 html/ 프로토타입 28개 파일*
